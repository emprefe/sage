# SAGE Flutter example

This is a small Flutter host for the reusable Dart Core package. It demonstrates participant update, canonical serialization, and error display without duplicating an image editor or CMS. Run it from this directory with `flutter pub get`, `flutter analyze`, `flutter test`, and `flutter run`.
