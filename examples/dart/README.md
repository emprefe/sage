# SAGE Dart reference

This package implements the v0.02 Core and a small CLI. It deliberately contains no Flutter or image-format dependency yet; it is the cross-language Core oracle that a future Flutter editor can use.
